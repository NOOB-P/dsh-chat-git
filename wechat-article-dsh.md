# 和 AI 忙了一下午，最怕的不是它笨，是它把你昨天弄好的那份弄没了

**—— 一个给所有 DeepSeek Harness 用户的插件：写代码的、写文档的、做日常工作的，都能存档、回退、留岔路口**

---

## 先说一个你可能已经历过的场景

你让 AI 帮你把一份会议纪要整理成正式格式。改得挺好，你满意地关掉了窗口。

第二天，你让它「在昨天那份基础上补一下这周的进展」。

它补了。顺便**把昨天你一句句抠出来的那几条结论，重写了一遍**。

你想退回昨天那版。然后你发现：

- 文件被改了好几个地方，你说不清哪一处是昨天定稿的；
- 聊天记录往上翻，你能看到自己说过什么，但**看不到当时那份文件长什么样**；
- 你只能凭印象再改一遍，改完还不确定是不是原来那版。

同样的事，换个活就是另一个版本：

- **简历**改到第五版，你让 AI「按这个思路再润一下」，它把前四版你谈好的措辞也一起换了；
- **给客户的方案**，你只想更新报价那一段，它顺手把整篇的章节顺序重排了；
- **整理了一下午的资料**，AI 回你「已按要求归类完成」，你翻了一遍，发现有两份不见了；
- **一份要交的表格**，你让它补一列，它把公式和格式一起改了，你连从哪一行开始错的都找不到。

**如果你是写代码的，这一幕你更熟：**

- 你只说「修一下这个空指针」，它改完顺手**把旁边三个文件也重构了**，`git diff` 一打开你眼前一黑；
- 重构到一半发现方向不对想回头，可对话已经聊了四十轮，**你根本说不出是从哪一句开始跑偏的**；
- 你前面明明强调过「不要引入新依赖」，聊了十几轮之后它忘了，你发现时它已经装了两个包。

共同点是同一个：**文件有后悔药，对话没有存档点。** 而用 AI 干活的时候，真正值钱的恰恰是那一整条对话——你的要求、你的纠正、你一步步把它掰回正轨的过程，全散在聊天框里，没有任何版本号。

`dsh-chat-git` 就是来补这一块的。

> 它不挑你干的是什么活。**写代码、写文档、整理表格、改简历、做方案、归档资料**——只要你在这个目录里干活，它都替你记着。

---

## 一句话说清它是什么

> **它让 AI 干的每一轮活，自动变成一个存档点；并且让「对话」和「文件」可以一起回退、一起留岔路口。**

它是 **DeepSeek Harness（DSH）** 的一个插件。装完重启，你的界面上就多两样东西：

1. 每条 AI 回复下面的图标行里，多一个**回退**按钮；
2. 标签栏里（**对话** / **轨迹** 旁边）多一个 **历史** 标签页。

就这样，没有别的学习成本。**零依赖、不用构建、不用改配置、不用学任何命令。**

### 同一套机制，两种活都吃

| | **AI coding** | **文档 / 日常工作** |
| --- | --- | --- |
| 你最想要的 | 代码能逐轮回滚，且和对话一起回去 | 知道哪一版是哪一版，改坏了能找回来 |
| 它给你什么 | 每轮一个检查点，`git log` 就是你的工作日志；**代码和对话一起回退** | 每轮一个存档，标题是「这轮干了什么」的人话；任意一版可还原 |
| 想试另一个方向 | fork 出去另开一条线，原线一行不动 | 同上，方案 A / 方案 B 各留一条 |
| 想说错话了重来 | 把那轮的要求原文捞回来改一改再问 | 一模一样 |

> 📸 **【截图 1｜封面图】**
> 截什么：打开 **历史** 标签页，把左右两栏完整截下来（左栏对话卡片 + 右栏文件存档列表）。
> 要求：一张图看懂「左边管对话、右边管文件」。左栏卡片上的三个按钮、右栏每条存档的标题都要能看清。这张当封面，信息密度越高越好。

---

## 能力一：AI 每干完一轮活，自动存一次档

你继续在对话框里干活，它在后面默默记账：

- **对话一开始**，它检查你当前的工作目录有没有一个「存档本」。没有？它自己建一个——但**只在当前这个目录里建**，绝不会把记录写到上层那个你根本没打开过的文件夹去；
- **每一轮结束**（AI 交付完成）时，它把这一轮的改动整体存一次档，标题长这样：

```
Ai-coding：把会议纪要整理成正式格式
Ai-coding：修复登录接口的空指针
```

注意标题**不是**把你那句几百字的原话塞进去，而是让模型读完「你要它做什么 + 它实际改了哪些文件」之后，**总结成一句 20 字以内的人话**。

所以你回头看的时候，读到的是一份**工作日志**，而不是一份聊天记录导出。**写代码的人尤其吃这一口**——`git log` 里躺着的是一句句「这轮干了什么」，而不是你自己手打的一堆 `update`、`fix`、`wip`。

> 📸 **【截图 2｜自动存档的效果】**
> 截什么：历史标签页的**右栏**，露出 5 条以上 `Ai-coding：…` 的存档标题。
> 要求：**这一段最好两类活各来几条**——既有「整理会议纪要」「把方案改成正式语气」这种文档活，也有「修复空指针」「重构设置页」这种代码活。这一张图同时说服两类读者：文档活它管，代码活它更管。全文最重要的一张「不挑活」证据。

**它不会因为模型抽风就丢掉存档。** 模型不可用、超时、报错，一律退化成用你的原话当标题——**少一个功能可以，丢一个存档不行。**

存多勤也能调：每轮一次（默认），或者放宽到每 2、3……10 轮一次。注意这个间隔**只影响存档，不影响对话**——对话历史始终是完整逐轮的，中间几轮的改动会攒着，右栏会老实告诉你「有未提交改动」。

> 📸 **【截图 3｜设置页】**
> 截什么：`设置 → 仓库增强` 整个面板。
> 要求：能看清「自动检查点」总开关、「自动保存间隔」下拉框、「总结模型」选择器，以及「检测」「下载」两个按钮。让人知道「这些东西都能自己调」。

---

## 能力二：一键回退——点两下，退回到那一轮

每一轮 AI 回复下方的图标行里（**紧挨复制按钮之后**）会多一个回退按钮：

1. **点第一下** → 进入待确认状态，按钮文案变化，**此时什么都不会发生**；
2. **点第二下** → 真的回退；
3. 鼠标移开或点别处 = 取消。

为什么要点两下？因为「退回去」是个不该轻率触发的动作，一次误点不该让你丢掉半天的活。

### 写代码的人注意：这个按钮回退的是**代码 + 对话**

不是二选一，是**一起**。点到哪一轮，那一轮之后改过的文件全部还原，对话也一起退到那个位置——**你回到的是「那一轮刚结束时的完整现场」**，而不是「代码回去了但对话还挂在那儿，你还得自己想清楚刚才聊到哪」。

这就是它和 IDE 里 `Ctrl+Z` 最大的区别：

| | `Ctrl+Z` | 这个插件 |
| --- | --- | --- |
| 退回粒度 | 一次编辑 / 一个文件 | **一整轮对话**（那次它到底干了什么） |
| 对话怎么办 | 不管，你得自己回忆 | **一起退**，位置永远对得上 |
| 能退多远 | 看你还按不按得动 | 从第一轮起，每一轮都在 |

而且关键在这里：**回退不会销毁你的原始对话。** 它是「另开一条只到那一轮的线，然后切过去」，原来那条被收进归档、随时能找回来。

**一次误点，不会让你永久丢失记录。**

> 📸 **【截图 4｜回退按钮】**
> 截什么：一条 AI 回复下方的图标行，用红框标出紧挨复制按钮的那个回退按钮。
> 要求：做左右对比——左边「未点击」原态，右边「点了一下、进入待确认」，最能说明「要点两下」。

> 📸 **【截图 5｜代码 + 对话一起回退，前后对比】**
> 截什么：做一张**前后对比图**。左边：回退前的状态（工作区里有它多改出来的文件 / `git status` 一片红）；右边：点完回退之后（文件回到那一轮的样子、对话也退到了同一轮）。
> 要求：这是**全文对 AI coding 读者最有杀伤力的一张**。哪怕只是「左边多出两个不该有的文件，右边没了」也比纯文字强十倍。建议再叠一行小字注明「对话也一起退到了第 N 轮」。

---

## 能力三：历史标签页——左边管对话，右边管文件

这是这个插件的核心界面。左右两栏**互不干扰**：文件那边读挂了，不会把对话列表一起带下去。

### 左栏 · 对话：你要找的「那一轮」都在这

- 按时间**倒序**排列，最新一轮在最上面，每轮带 **年月日 + 时分秒** 的时间戳（`2026-09-13 14:05:32`）——只写 `14:05` 你根本分不清是今天还是上周，而两轮落在同一分钟里是常事；
- 最上面那轮带一个 **`当前位置`** 标签，列表上方还有一行「当前位置：第 N 轮。」——列表一滚动，「最上面那行」就不再是答案；
- 那一轮如果带过图片（截图、设计稿、报错截图），卡片上会标一行 **`含 N 张图片`**；
- 每张卡片三个动作：

| 按钮 | 它做什么 | 什么时候用 |
| --- | --- | --- |
| **从这里打开新对话(fork)** | 另开一条只到这一轮的对话并切过去，**原对话原样保留**，两条线都能继续 | 想试另一个方向，又舍不得丢掉现在这条 |
| **回退到这里(还原)** | 同上，但**原对话随即归档**（可从归档恢复） | 就是走错了，要往回走 |
| **编辑并发送** | 弹窗选去哪儿，把这一轮**完整的要求**（文字 + 图片）放进新对话的输入框 | 想改个说法再问一遍（见下一节） |

> 📸 **【截图 6｜历史左栏】**
> 截什么：历史页左栏的对话卡片列表。
> 要求：一定要有一张卡片**同时**具备 `当前位置` 标签、完整时间戳、`含 N 张图片`、三个按钮——这一张图就能把左栏的信息量全说清。

正在跑、还没结束的那一轮**不能分叉**，按钮会变灰并说明原因。因为猜一个位置会开出错误的线，那比不给按钮更糟。

### 右栏 · 文件存档：AI 到底动过什么

这里直接读你工作目录真实的记录：当前分支、工作区是否干净、最近 100 条存档（**包括不是这个插件写的**，也就是你自己手动存的那些也在），每条带作者和时间。

点某条的 **还原到这里**，会先弹一个确认框，把话说全：

- 要还原到哪一版（编号 + 标题）；
- **提交历史不动**（只把文件内容写回去，不改写你的存档记录）；
- **没进过存档的文件不会被清理**（不属于任何存档的文件，删了就找不回来，所以它不碰）；
- `当前位置：xxxxxxx → 还原后变成 xxxxxxx`。

**确认之前不会发任何请求**，`取消` 是真正的一等出口。

> 📸 **【截图 7｜还原确认弹窗】**
> 截什么：右栏点「还原到这里」后弹出的确认框。
> 要求：要能看清标题、以及底部那行「当前位置 → 还原后变成」。框里有两句术语是界面原样写的（`HEAD 不动`、`未跟踪`），**不用改图，照截即可**——这两行恰恰是「它不会顺手删你的东西」的书面承诺。这张图最能证明它做事有多谨慎。

还原后，你当前所在的那一行会带上 **`当前位置`** 标签，按钮变成不可点的 `已在此位置`——还原到自己已经站着的地方，是个只会报成功的空操作，所以它干脆不给你点。

> **写文档的人注意**：这一栏读的是**整个目录**的真实历史，所以你手动存的那些版本也在这儿。以前你靠 `方案_v3_最终版_真的最终.docx` 记版本，现在这里能看到「哪一版是哪一版」。

---

## 能力四：编辑并发送——想改个说法再问一遍，不必重打一遍

这是我自己用得最多的功能。

**文档活的场景**：三轮之前你说「把这份材料改成正式一点的语气」，现在你想改成「改成正式语气，并且把结论提到最前面」。

**代码活的场景**：前面你说「修一下这个空指针」，现在你想改成「修空指针，并且补一个单元测试」。

两种活都不必往上翻十屏找那句话再复制。直接在历史左栏找到那一轮，点 **编辑并发送**。

它会先弹窗问你**去哪儿**：

> 📸 **【截图 8｜编辑并发送的三选一弹窗】**（全文最关键的一张）
> 截什么：点「编辑并发送」后弹出的对话框，包含 `当前对话` / `新建对话` / `取消` 三个按钮。
> 要求：**连上方那段说明文字一起截**，别只截按钮。

- **当前对话（还原）** —— 你眼前这个窗口**变成**重做后的那条线（同样在「上一轮」处切开），原对话随即归档，标题沿用不变。相当于「这条对话退一步，然后我重新提要求」；
- **新建对话（fork）** —— 在它旁边另开一条并切过去，原对话**保持原样**，两条线都能继续，新线会自动拿到 `… (1)` 这样能区分的标题。相当于「留个岔路口，我去旁边试试」；
- **取消** —— 什么都不做：不读内容、不建对话、不归档。

两者的唯一区别就是**你正站着的这条对话还留不留下**。而这件事在动作发生之后就看不出来了，所以它必须在发生**之前**问你一次。

选完之后，它会：

1. 取回那一轮**完整的要求原文**（不是卡片上那段被截短的展示文字——把截断的文字交回去，等于让你对着一个自己从没提过的要求继续）；
2. 重做对话（如果是第一轮，就在同一个工作目录里**新建一个对话**）；
3. 把整段要求写进新对话的**输入框**，光标在那里等你。

**它不会替你按发送。** 想改就改，想换个模型就用界面上的模型选择器，不想发就删掉。发送键本身就是确认。

> 📸 **【截图 9｜要求已经躺在输入框里】**
> 截什么：执行完「编辑并发送」之后的新对话界面，输入框里已经填好一整段要求（如果那轮带图，还要看到图片挂在输入区）。
> 要求：**必须让输入框处于「已填好但未发送」的状态**——这是这个功能最核心的一句话：它把控制权交还给你。

**图片也会一起带过去。** 新线是在「那一轮之前」切开的，那张图不在新对话的记录里，所以插件会用**原对话**的授权把图片重新读回来，再和文字一起放进输入框。你贴在对话里的**报错截图、设计稿、参考图**，重问一遍时不用重新翻出来上传。极端情况下读不回图片，它会放弃图片、文字照常送达——**一个少了图的请求，总比一个根本交不过去的请求有用。**

---

## 给 AI coding 工作者：它补的是「AI 写代码最缺的那一环」

如果你日常就是用 AI 写代码、改 bug、做重构，那这一节是专门给你的。前面那些能力落到代码活上，实际解决的是四件事：

**1. 代码和对话第一次能一起回退。**
现在的常态是：代码能 `git reset`，对话只能自己往上翻。于是「回到刚才那个状态」永远是半截的——代码回去了，你还得回忆当时跟它说过什么。它把这两件事绑在同一个动作上。

**2. 每一轮都是可回溯的检查点，而不是一个大 diff。**
聊了四十轮之后你最怕的就是 `git diff` 一屏翻不完。这里你看到的是四十个检查点，每个带着一句「这轮干了什么」。**想找「它从哪一轮开始把架构改歪的」，答案就摆在那里。**

**3. 试错变成一条岔路，而不是一次赌博。**
以前你想「换一种实现试试」，就得先把当前代码 stash 掉，聊崩了再 stash pop 回来，来回折腾还容易搞丢。现在是 **`从这里打开新对话(fork)`**——另开一条线去试，原线一行没动，试完觉得好就留下，不好就切回去。

**4. 它没遵守你的约束时，你能精确地重来一次。**
「不要引入新依赖」「保持这个函数签名」「只改这一个文件」——这些话聊到第十轮就容易被忽略。用 **`编辑并发送`** 回到当初提要求的那一轮，把约束**补进原文**，再问一遍。比在对话末尾吼一句「我不是说了不要加依赖吗」有用得多。

> 📸 **【截图 10｜给 coding 读者的补充图】**
> 截什么：历史左栏里，几条**明显是代码活**的卡片（比如「修复登录空指针」「重构设置页开关」），最好带上那个 `含 N 张图片` 标记（对应你贴过报错截图的那一轮）。
> 要求：放在这一节附近。让写代码的读者一眼确认「这玩意儿真的在代码活里跑过」，而不是给办公场景做的通用工具。

---

## 怎么装（三种方式选一种）

**前置条件**：Node.js ≥ 22、一个能用的 DSH profile（下面以 `web` 为例）、git（可选但强烈建议，没装的话设置页会给你官方下载入口）。

### 方式一：从 GitHub 直接装（推荐）

```bash
dsh plugin --profile web add github:NOOB-P/dsh-chat-git
```

### 方式二：不下载，直接从 Release 资产装

```bash
dsh plugin --profile web add https://github.com/NOOB-P/dsh-chat-git/releases/download/v0.6.0/dsh-chat-git-0.6.0.tgz
```

### 方式三：先下载 `.tgz`，再从本地装

到 [Releases](https://github.com/NOOB-P/dsh-chat-git/releases/latest) 下载 `dsh-chat-git-0.6.0.tgz`，然后：

```bash
dsh plugin --profile web add ./dsh-chat-git-0.6.0.tgz
```

**装完（或更新后）需要重启 `dsh web`** 才会加载。想卸载：

```bash
dsh plugin --profile web remove dsh-chat-git
```

> 📸 **【截图 11｜安装过程】**
> 截什么：终端里执行上面任意一条 `dsh plugin --profile web add ...` 命令，以及跑完后的输出。
> 要求：截到命令本身 + 成功回显，让人觉得「就一行」。

> 📸 **【截图 12｜装好之后的样子】**（建议放在安装段结尾）
> 截什么：重启后，标签栏里多出来的那个 **历史** 标签页。
> 要求：把标签栏单独放大截一张，圈出「历史」。给一个「装好你会看到这个」的落点。

---

## 它适合谁

**适合你，如果：**

- 你用 AI 帮忙写东西、改文件、整理资料，而且**不止一次**想「回到它乱改之前」；
- **你用 AI 写代码**，想让代码和对话能一起回到某一轮，而不是只回退半截；
- 你不想学一堆命令，但**希望有人替你记着**每一步干了什么；
- 你想把一个方向**留着当岔路口**，而不是二选一删掉；
- 你手上那些反复改的文档（方案、材料、纪要、简历、表格），你希望有一个「哪一版是哪一版」的底气。

**可能不适合你，如果：**

- 你不在 DeepSeek Harness 里干活（这是 DSH 插件，不是通用工具）；
- 你的文件放在当前工作目录之外（它只管你正在干活的这个目录）；
- 你的工作目录绝对不能自动生成存档结构。

---

## 常见疑问

**Q：我不写代码，也能用吗？**
能。它不看你干的是什么活，只看你在这个目录里改了什么文件。代码、文档、表格、笔记一视同仁。

**Q：我写代码，它和 git 是什么关系？会不会和我的工作流打架？**
它是**叠加**在你的 git 之上的，不替换任何东西。你的分支、你的提交、你的远程仓库照旧；它只是在每轮对话结束时多打一个带语义标题的提交，并且额外记住「这一轮对应哪次提交」。你手动写的提交它也照读照显示。

**Q：存档标题前面为什么写着 `Ai-coding：`？**
那是标题的固定前缀，后面那句才是模型总结出来的这轮干了什么。前缀只是让这些自动存档一眼能和别的东西区分开。

**Q：回退之后，原来的对话去哪了？**
归档了，不是删了。`回退到这里(还原)` 会另开一条只到那一轮的对话并把原对话归档，可以从归档里恢复；`从这里打开新对话(fork)` 则原样保留两条线。

**Q：它会不会把我没让它管的文件删掉？**
不会。没进过存档的文件它完全不碰——那些文件不属于任何存档，删了就找不回来，所以它选择不碰。（写代码的人尤其在意这条：你那些没提交的实验代码不会被顺手清掉。）

**Q：它会不会在我电脑上乱建东西？**
它会在你的工作目录里建一个存档结构（就是 git 仓库，一个隐藏文件夹），这是它记账的地方。它**只在当前目录建**，不会往上层的文件夹里写。

**Q：一定要装 git 吗？**
不装也能用，但没有存档功能：检测不到 git 时「自动检查点」开关会被锁住，并给你官方下载入口。git 是唯一的外部依赖，而且是**装在系统里的**，不是 npm 依赖。

**Q：重启 DSH 之后，历史还能回退吗？**
能。记录写在 `$DSH_HOME/chat-git.json` 里，不在内存里。写失败会降级成只在内存里记，不会把插件搞崩。

**Q：会不会拖慢我？**
它零依赖、不用构建。唯一的额外等待是每轮结束会等一次模型调用生成标题，**上限 8 秒**，而且读不到模型就直接用你的原话当标题——绝不会因为模型不可用而丢存档。

**Q：我不小心点了「当前对话」，原对话还在吗？**
在。它进的是归档，可以从归档里恢复出来。这也是为什么弹窗要问你一次，而不是替你决定。

---

## 最后

代码的版本管理我们做了二十年，做得很好。但**「我用 AI 干过的活」的版本管理，几乎没有**——而那个过程，才是决定成败的东西。

不管你是写代码的，还是用 AI 处理文档、表格、方案的，`dsh-chat-git` 想补的都是同一块：**让你第一次能对得上「我刚才到底让它做了什么」和「它到底改了什么」，并且随时能退回去。**

- GitHub：**https://github.com/NOOB-P/dsh-chat-git**
- Release（v0.6.0）：**https://github.com/NOOB-P/dsh-chat-git/releases/tag/v0.6.0**

一行命令，重启，标签栏里就多一个「历史」：

```bash
dsh plugin --profile web add github:NOOB-P/dsh-chat-git
```

MIT 协议，欢迎 issue 和 star。