# AI 把代码改崩了，难道只能重讲一遍需求？

**—— 我给 DeepSeek Harness 写了个插件，让「对话」和「代码」一起存档、一起回退**

---

## 先说一个你肯定遇到过的场景

凌晨一点，你跟 AI 说：「把登录接口改成返回 `{ ok: true }`，顺手补个失败分支的测试。」

它照做了。你又说：「那顺便把错误码也统一一下。」

它又照做了，顺便**把三个你没让它动的文件也改了**。

你盯着 `git diff` 想骂人。但更麻烦的是——你连「它到底从哪一步开始跑偏」都说不清。你想退回去，于是：

- `git reset` 一遍，代码回到某个不知道对不对的状态；
- 对话窗口往上翻十屏，把当初那句提示词**重新打一遍**；
- 祈祷这次它别再乱改。

**代码有版本管理，但「对话」没有存档点。** 而 vibe coding 里真正值钱的东西恰恰是对话——你的意图、你的约束、你一步步纠偏的过程，全散在那条聊天记录里，没有任何版本号。

---

## 所以我写了 dsh-chat-git

一句话：**它让 AI 编程的每一轮对话，自动变成一个 git 提交；并且让「对话」和「代码」可以一起回退、一起分叉。**

> 📸 **【截图 1｜封面/头图】**
> 截什么：打开插件的**「历史」标签页**，把左右两栏完整截下来（左栏对话卡片列表 + 右栏 git 提交列表）。
> 要求：能看清左栏卡片上的三个按钮 `从这里打开新对话(fork)` / `回退到这里(还原)` / `编辑并发送`，以及右栏每条提交的短 sha 与主题。这张图当封面，信息密度高一点最好。

它是 **DeepSeek Harness（DSH）** 的一个插件。DSH 是一个可扩展的 AI 编程运行环境，而这个插件是挂上去的一层「存档 + 回溯」能力，**零 npm 依赖、没有构建步骤**，装完重启就生效。

---

## 能力一：每一轮对话，自动一个提交

你在对话框里干活，它在后台记账：

- **对话开始**时，检查这个会话的工作目录是不是一个 git 仓库。不是？那就 `git init` 一个，但**只在这个目录里**，绝不会把提交打到上层那个你根本没打开过的仓库去。
- **每一轮对话结束**（agent 交付完成）时，`git add -A -- .` + `git commit`，提交信息长这样：

```
Ai-coding：修复设置页开关无法启用
```

注意标题不是把你那句几百字的提示词原样塞进去，而是**让模型读完提示词 + `git diff --cached` 的改动摘要之后，总结出一句 20 字以内的人话**。所以 `git log` 读起来像一份工作日志，而不是一份聊天记录导出。

> 📸 **【截图 2｜自动提交】**
> 截什么：在终端里跑 `git log --oneline -10`，截出那串 `Ai-coding：xxx` 的提交列表。
> 要求：至少露出 5 条以上，最好包含一条明显是「改 bug」的、一条「加功能」的，能看出标题是人话而不是提示词原文。

**它不会因为模型挂了就丢检查点。** 模型不可用、超时、报错，一律回退成提示词本身当标题——**少一个功能可以，丢一个存档不行**。

提交频率也能调：每轮一次（默认），或者放宽到每 2、3……10 轮一次。注意这个间隔**只节流 git，不节流对话**——对话历史始终是逐轮完整的，中间几轮的改动就攒在工作区里，右栏会如实显示成「有未提交改动」。

> 📸 **【截图 3｜设置页】**
> 截什么：`设置 → 仓库增强`，把整个设置面板截下来。
> 要求：要能看清「自动检查点」总开关、「自动保存间隔」下拉框、「总结模型」选择器，以及「检测」「下载」两个按钮。

---

## 能力二：回合图标行多了一个「回退」按钮

每一轮 AI 回复下方的图标行里（**紧挨复制按钮之后**）会多出一个回退图标：

1. **第一次点击** → 进入待确认状态，按钮文案变化，此时什么都不会发生；
2. **第二次点击** → 真的回滚；
3. 鼠标移开或点别处 = 取消。

> 📸 **【截图 4｜回退按钮】**
> 截什么：一条 AI 回复下方的图标行，用红框/箭头标出**紧挨复制按钮**的那个回退图标。
> 要求：一张「未点击」的原态，一张「点了第一下、进入待确认」的状态，做左右对比图最好。

这里有个关键设计：**回滚走的是 `sessions.fork`，原始会话不会被销毁**，随时可以找回来。DSH 里没有「截断会话」这种 API，所以「把后面的对话删掉」实际实现为：在这一轮的结束序列处分叉出一个只含前 N 轮的新会话，切过去，原会话归档（可恢复）。

**一次误点，不会让你永久丢失记录。**

---

## 能力三：「历史」标签页——左边管对话，右边管代码

标签栏里会多出一个 **历史** 页，左右两栏，**两栏互不读写**（仓库坏掉不会带走对话列表，反之亦然）。

### 左栏 · 对话

- 按时间**倒序**列出每一轮，最新一轮在最上面，带 **年月日 + 时分秒** 时间戳（`2026-09-13 14:05:32`）；
- 最上面那轮带一个 **`当前位置`** 标签，列表上方另有一行「当前位置：第 N 轮。」——列表一旦滚动，「最上面那行」就不再是答案；
- 如果那一轮带过图片，卡片上会标一行 **`含 N 张图片`**；
- 每张卡片三个动作：

| 按钮 | 它做什么 |
| --- | --- |
| **从这里打开新对话(fork)** | 新开一条只到该轮的会话并切过去，**原对话保持原样**，两条线都能继续 |
| **回退到这里(还原)** | 同上，但**原对话随即归档**（可从归档恢复），相当于往回走 |
| **编辑并发送** | 弹窗选去向，把该轮**完整请求**（文字 + 图片）放进新会话的输入框 |

> 📸 **【截图 5｜历史左栏】**
> 截什么：历史页左栏的对话卡片列表。
> 要求：一定要有一张卡片同时具备 `当前位置` 标签、完整时间戳、`含 N 张图片` 标记和三个按钮——这一张图就能把左栏的信息量全说清。

正在进行中、还没有结束序列的那一轮**无法分叉**，按钮置灰并说明原因。猜一个边界会产出错误的 fork，比不给按钮更糟。

### 右栏 · 工作区 Git

直接读仓库真实历史：分支、HEAD、工作区是否干净，最近 100 条提交（**包括不是这个插件写的提交**），每条带作者和时间。

点某条的 **还原到这里** → 先弹出确认对话框，写明：

- 要还原到哪个提交（id + 主题）；
- **`HEAD 不动`**；
- **未跟踪文件不会被清理**；
- `当前位置：xxxxxxx → 还原后变成 xxxxxxx`。

确认前**不发任何请求**，`取消` 是真正的一等出口。

> 📸 **【截图 6｜还原确认弹窗】**
> 截什么：右栏点「还原到这里」后弹出的确认对话框。
> 要求：要能看清提交主题、`HEAD 不动`、`未跟踪`、以及底部那行「当前位置 → 还原后变成」。这张图是「它有多谨慎」的最好证明。

还原后，工作区当前所在的那一行会带上 **`当前位置`** 标签，按钮变成不可点的 `已在此位置`（还原到自己已经站着的地方是个只会报成功的空操作）。

**还原只动工作区**：不改对话、不移动 HEAD、不丢检查点。

---

## 能力四：「编辑并发送」——把这一轮重新问一遍，但不必重打一遍

这是我自己用得最多的功能。

点历史卡片上的 **编辑并发送**，会先弹一个窗，问你**去哪儿**：

> 📸 **【截图 7｜编辑并发送的三选一弹窗】**
> 截什么：点「编辑并发送」后弹出的对话框，包含 `当前对话` / `新建对话` / `取消` 三个按钮。
> 要求：连同上方那段说明文字一起截，别只截按钮。这张图是全文最关键的一张。

- **当前对话（还原）** —— 本窗口**变成**重建后的那条线（同样在「上一轮」处切开），当前对话随即**归档**，标题沿用不变；
- **新建对话（fork）** —— 在它旁边另开一条只到「上一轮」的会话并切过去，当前对话**保持原样**，两条线都能继续，新线自动拿到 `… (1)` 这样的可区分标题；
- **取消** —— 什么都不做：不读请求、不建会话、不归档。

两者的唯一区别就是**你正站着的这条对话还留不留下**。而这件事在动作发生之后无法从界面上读出来，所以它必须在发生之前被问一次。

选完去向之后，它会：

1. 从会话日志里取回这一轮**完整的原始请求**——注意不是卡片上那段被截到 300 字的展示文本，把展示用的截断文本交回去，等于让你对着一个自己从没写过的问题继续；
2. 重建对话（第 1 轮没有「上一轮」，就在同一个工作区**新建一个会话**）；
3. 把整段请求写进新会话的**输入框**，光标在那里等你。

**它不会替你发送。** 想改就改，想换模型就用 shell 自己的模型选择器，不想发就删掉。发送键本身就是确认。

> 📸 **【截图 8｜提示词已被填进输入框】**
> 截什么：完成「编辑并发送」之后的新会话界面，输入框里已经躺着一整段提示词（如果那轮带图，还要能看到图片缩略图挂在输入区）。
> 要求：**要让光标/输入框处于「已填好但未发送」的状态**，这是这个功能最核心的一句话：它交还控制权，不替你按发送。

**图片也会一起带过去。** 新线是在「这一轮之前」切开的，那张图的持久引用并不在新会话的日志里，所以它授权不了自己的字节——插件会用**原会话**的 `readAttachment` 把字节读回来，再注册成新会话的草稿图片，和文字一起写进输入框。部署里没挂图片服务时会放弃图片、文字照常送达：一个少了图的请求，仍然比一个根本交不过去的请求有用。

---

## 给专业 vibecoding 的人：几个刻意的设计取舍

如果你自己写插件、读源码，这几条可能是你在意的：

1. **回退用 fork，不销毁会话。** 原始日志保留，用户不会因为一次误点永久丢记录。
2. **`git checkout` 语义是「还原内容 + 清掉新增」，HEAD 不动。** 单条 `git checkout <sha> -- .` 不会删掉该提交中不存在的路径，后面几轮新建的文件会残留、让回溯看起来只做了一半，所以之后还会 `git rm` 掉区间内新增的路径。HEAD 保持不动，让「轮次 → 检查点」的映射继续可读。
3. **未跟踪文件不碰。** 它们不属于任何检查点，删掉就找不回来。
4. **git 走 argv 数组，不走 shell 字符串。** 没有引号规则要处理、提交信息与路径没有注入面；sha 进 argv 之前先按对象名形状校验，再 `rev-parse --verify <sha>^{commit}` 解析成完整 id——不做路径拼接，也不可能把 `--upload-pack=…` 这类字符串当选项传下去。
5. **`cwd` 绝不来自请求体。** 只来自宿主自己知道的来源（状态文件记账 + 会话头的 `header.cwd`），否则这条路由就成了任意目录读取器。
6. **历史数据来自会话日志，不来自前端已渲染的内容。** 所以早已滚出窗口、甚至进程重启前发生的那一轮，同样拿得到可用的分叉边界。
7. **转交靠一个无渲染的席位。** 新会话的输入区属于另一个插件，而且 `sessions.open` 是请求不是同步切换——请求按 session id 寄存，由那个席位在**它自己那一轮的输入区挂载时**取走，只通过 shell 公布的 `setDraft` / `addImages` 写入，绝不去碰另一个插件的内部状态。取走即删除，中途切走再切回来不会覆盖你已经打好的内容。

**这个插件不再写「默认模型」。** 旧版本为了让重建的新线跑在选定模型上，会去写部署的全局默认选择——一个历史卡片上的动作改掉整个部署的模型，而模型选择器对此毫不知情。既然现在不再自动发送，这个时机就消失了，那条路由被整个删掉。**留着一个没人调用的全局写入口，比删掉更危险。**

---

## 怎么装（三选一）

**前置要求**：Node.js ≥ 22、一个可用的 DSH profile（下面以 `web` 为例）、git（可选但强烈建议，没装的话设置页会给你官方下载入口）。

### 方式一：从 GitHub 直接装（推荐）

```bash
dsh plugin --profile web add github:NOOB-P/dsh-chat-git
```

pnpm 会把仓库拉进 profile 依赖，`dsh` 随后按已安装状态重建层列表，不必手动 clone。

### 方式二：不下载，直接从 Release 资产装

```bash
dsh plugin --profile web add https://github.com/NOOB-P/dsh-chat-git/releases/download/v0.6.0/dsh-chat-git-0.6.0.tgz
```

### 方式三：先下载 `.tgz`，再从本地文件装

从 [Releases](https://github.com/NOOB-P/dsh-chat-git/releases/latest) 下载 `dsh-chat-git-0.6.0.tgz`，然后：

```bash
dsh plugin --profile web add ./dsh-chat-git-0.6.0.tgz
```

**装完（或更新后）需要重启 `dsh web`** 才会加载。卸载：

```bash
dsh plugin --profile web remove dsh-chat-git
```

> 📸 **【截图 9｜安装过程】**
> 截什么：终端里执行上面任意一条 `dsh plugin --profile web add ...` 命令，以及跑完后的输出。
> 要求：截到命令本身 + 成功回显，让人觉得「就一行」。

> 📸 **【截图 10｜装好之后的位置】**
> 截什么：重启后，标签栏里多出来的那个 **历史** 标签页（`对话` / `轨迹` 旁边）。
> 要求：把标签栏单独放大截一张，圈出「历史」。这张适合放在安装段的结尾，给一个「装好了你会看到这个」的落点。

---

## 它适合谁

**适合你，如果：**

- 你用 AI 写代码，且**不止一次**想「回到 AI 乱改之前」；
- 你的项目本来就该有 git，但 AI 每一轮改了什么、为什么改，你其实说不清；
- 你想把一个跑偏的方向**留个岔路口**，而不是二选一删掉。

**可能不适合你，如果：**

- 你不在 DSH 里写代码（这是个 DSH 插件，不是通用工具）；
- 你的项目已经在用非常严格的 PR / 分支流程，不需要 AI 自动提交；
- 你的工作目录不能随便 `git init`。

---

## 常见问题

**Q：回退之后原来的对话去哪了？**
归档了，不是删了。`回退到这里(还原)` 会新建一条只到该轮为止的会话并把原会话归档，可以从归档里恢复；`从这里打开新对话(fork)` 则原样保留两条线。两者在 DSH 里都是同一个 `sessions.fork` 原语，区别只在原会话的去向。

**Q：它会把我的文件删掉吗？**
未跟踪文件不碰。它们不属于任何检查点，删掉就找不回来，所以插件选择不碰它们。

**Q：一定要装 git 吗？**
不装也能用，但没有任何检查点功能：检测不到 git 时自动检查点开关会被锁住，并给你官方下载入口。git 是唯一的外部依赖，而且是**进程级**的，不是 npm 依赖。

**Q：harness 重启后历史还能回退吗？**
能。`sessionId → { cwd, position, commits }` 的映射写在 `$DSH_HOME/chat-git.json` 里，不在内存里。写失败会降级为仅内存，不会把插件拖垮。

**Q：装它会不会拖慢我？**
它零 npm 依赖、无构建步骤。唯一的额外等待是每轮结束会等一次模型调用来生成提交标题，**上限 8 秒**，且读不到模型时直接回退成提示词——绝不会因为模型不可用而丢检查点。

---

## 最后

代码的版本管理我们做了二十年，做得很好。但**对话的版本管理几乎没有**——而对话，才是 AI 编程里那个真正决定成败的东西。

`dsh-chat-git` 想补的就是这一块：**让「我刚才到底让它做了什么」和「它到底改了什么」这两件事，第一次能对得上，并且随时能退回去。**

- GitHub：**https://github.com/NOOB-P/dsh-chat-git**
- Release（v0.6.0）：**https://github.com/NOOB-P/dsh-chat-git/releases/tag/v0.6.0**

一行命令，重启，标签栏里就多一个「历史」：

```bash
dsh plugin --profile web add github:NOOB-P/dsh-chat-git
```

MIT 协议，欢迎 issue 和 star。